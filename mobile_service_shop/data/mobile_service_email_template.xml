<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">
        <!--Email template -->
        <record id="email_template_mobile_service" model="mail.template">
            <field name="name">Estado de su reparación</field>
            <field name="model_id" ref="mobile_service_shop.model_mobile_service"/>
            <field name="email_from">${(object.user_id.email |safe}</field>
            <field name="email_to" >${object.person_name.email |safe}</field>
            <field name="subject">Ref ${object.name or 'n/a' }</field>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.person_name.lang}</field>
            <field name="body_html"><![CDATA[
                <p>Estimadx ${object.person_name.name},</p>
                <p>Su solicitud de servicio for ${object.brand_name.brand_name or 'n/a' } (${object.model_name.mobile_brand_models or 'n/a' }) con número de referencia <b>${object.name or 'n/a' }</b> ha sido procesada a la etapa de: <b>${object.service_state or 'n/a' }</b>.</p>
                <p>Cualquier duda o aclaración, por favor haganos saberlo a través de WhatsApp haciendo clic aquí: https://wa.me/5217294657180 </p>
                <p>Saludos!!</p>]]>
            </field>
        </record>

    </data>

</odoo>